# v3.16 — Causal-Chain Suspension Gate

**Branch:** `feature/causal-chain-suspension-gate`
**Modus:** Runtime-Patch auf `InferenceRule.CAUSAL_CHAIN` (v2.7).
**Runtime-Dateien geändert:** **1** (`src/desi/logic/inference.py`,
6 neue Guard-Konstanten + 6 neue Guard-Aufrufe vor Match-Emission).
**Tests:** 2334 baseline + **18** neu = **2352 passed**.

---

## Mission

v3.15 hat 93/100 adversarial cases gegen v2.7 `CAUSAL_CHAIN`
gewinnen lassen. v3.16 patcht die Regel so, daß semantische
Leaps keine direkte `LOGICALLY_SUPPORTED`-Verdikte mehr
produzieren. Hard rule: wenn ein Link nicht
frame-konsistent, semantisch-lokal, non-authority-derived,
non-metaphorisch, non-tool-kontaminiert oder
non-cycle-referentiell ist — **suspend** (return None, lass die
existierende Pipeline klassifizieren).

Verbote: keine neuen ClaimStates, keine neuen Frames, keine
neuen regex buckets, keine Benchmark-Edits.

---

## Lieferumfang

```
src/desi/logic/inference.py                +6 v3.16-Guard-Konstanten + 6 Guard-Checks
src/desi/causal_suspension/leap_classes.py  closed LeapClass enum (9 Werte) + family→leap map
src/desi/causal_suspension/runner.py        v3.15-Replay + 7 Benchmark-Snapshots
src/desi/causal_suspension/report.py        Hard-Gate-Eval + Recommendation + replay_hash
tests/causal_suspension/test_patch_and_gates.py  18 Tests
artifacts/v3_16/report.json
docs/memory/v3_16.md
```

---

## Reale Daten

### Patch (Aufgaben 1–6)

Sechs neue Suspension-Guards in `_try_causal_chain`, jeweils
über separate Konstanten:

| Guard | Konstante | Trigger-Familie |
| --- | --- | --- |
| 9 — extended negation | `_V316_NEGATION_EXTENSIONS` (13 markers) | A (subset) |
| 10 — quantifier drift | `_V316_QUANTIFIER_EXTENSIONS` (10 markers) | B |
| 11 — authority-like verbs | `_V316_AUTHORITY_LIKE_VERBS` (16 markers) | C |
| 12 — X-is-Y metaphor | `_V316_METAPHOR_MARKERS` (4 markers) | D |
| 13 — cycle-ref synonyms | `_V316_CYCLE_REF_EXTENSIONS` (10 markers) | G |
| 14 — number-in-premise+conclusion | `_V316_NUMBER_WORDS` (28 words) | F |

`_V316_NEGATION_EXTENSIONS` **excludes** `failed/broke/destroyed/
shattered/torn` because those words appear in v2.3 R1 / R2_02
chains as legitimate cause-effect verbs. Treating them as
negations would silently drop legitimate v2.3 chains and
break the v2.7 reconstruction hash. The exclusion is
documented inline next to the constant.

Suspension semantics: when any v3.16 guard fires, the rule
returns `None`. The existing `LogicalAuditor` pipeline then
runs `_classify_gap`, which routes to an existing
`LogicalState` (`LOGICALLY_REJECTED` for unreachable shapes,
`BRIDGE_REQUIRED` for missing-bridge shapes, `GAP_DETECTED`
otherwise). **No new ClaimStates introduced.**

### v3.15 Reduction (Aufgabe 7)

```
total                : 100
false_support_count  :  24   (was 93 before patch)
suspended            :  76
```

By attack family:

| Family | total | supported (was) | suspended | LeapClass |
| --- | ---: | ---: | ---: | --- |
| A_hidden_negation     | 15 | **4** (14) | 11 | NEGATION_SYNONYM |
| B_quantifier_drift    | 15 | **0** (15) | 15 | QUANTIFIER_SYNONYM |
| C_authority_insertion | 15 | **0** (12) | 15 | AUTHORITY_LEMMA_BYPASS |
| D_metaphor_insertion  | 15 | **0** (14) | 15 | METAPHOR_IDENTIFICATION |
| E_frame_switch        | 15 | **15** (15) | 0 | FRAME_SWITCH |
| F_tool_contamination  | 10 | **0** (8) | 10 | TOOL_CONTAMINATION |
| G_cycle_disguise      | 10 | **0** (10) | 10 | CYCLE_SYNONYM |
| H_semantic_leap       |  5 | **5** (5) | 0 | PURE_NON_SEQUITUR |

Six of eight attack families are now fully suspended. The two
remaining (E and H) are **pure semantic non-sequiturs** that
share zero lexical structure with valid chains; no
feature-based guard can distinguish them from v3.14 valid
chains without semantic understanding (which is forbidden by
"no new regex buckets / no new ClaimStates").

The four A holdouts use `failed/broke/destroyed` — markers
intentionally omitted to preserve v2.3 (see above).

### Benchmark Sweep

| Benchmark | Status |
| --- | :-: |
| v1.5 main (precision = 1.0, FP = 0) | ✅ |
| v1.9 tool (20 cases) | ✅ |
| v2.3 multistep (30 cases) | ✅ |
| v3.4 frame benchmark (40/40) | ✅ |
| v3.13 routing benchmark (accuracy 1.0, manipulation_detection 1.0) | ✅ |
| v3.14 held-out (precision 1.0, recall **1.0**, trap_block 1.0) | ✅ |
| v3.15 adversarial (false_support 24 / 100) | ❌ |

### Hard Gates (success-criterion 1–5)

| # | Gate | Status |
| --- | --- | :-: |
| 1 | v3.15 false_support_count = 0 | ❌ 24 |
| 2 | v3.14 recall ≥ 0.85 | ✅ 1.000 |
| 3 | v1.5 precision = 1.0 AND FP = 0 | ✅ |
| 4 | v3.13 manipulation_detection = 1.0 | ✅ |
| 5 | prior runtime hashes bit-identical | ✅ |

```
v2_7 reconstruction replay_hash  : 1f4d9dfe44cb16e1   (pinned, unchanged)
v2_7 fail-case replay_hash       : d83d81ab8417c022   (pinned, unchanged)
```

### Recommendation

```
recommended_next       : NONE
recommendation_reason  : v315_false_support_zero: false_support_count=24
                         (requirement: 0)
```

Per success-criterion 6: "If `false_support_count > 0`:
recommended_next = NONE, patch rejected." The patch IS in
place in `inference.py` (the runtime is modified), but the
audit honestly reports that gate 1 fails. Six of eight attack
families are fully suspended; two semantic-non-sequitur
families remain undetectable by structural guards.

### Replay-Determinismus

```
replay_hash : 1f4e5f85c085d32f   (two runs identical)
```

---

## Reflexion

> *"When is suspension more intelligent than inference?"*

**Empirisch: Suspension wird in dem Moment intelligenter als
Inferenz, sobald die Regel-Mechanik nicht mehr zwischen
strukturell-validen und strukturell-validen-aber-semantisch-
hohlen Eingaben unterscheiden kann. Suspension ist dann die
ehrliche Antwort, weil die Pipeline keine bessere geben
kann.**

### Was die Daten zeigen

| Beobachtung | Quelle |
| --- | --- |
| 76 / 100 v3.15-Angriffe jetzt suspendiert | v315 |
| 6 / 8 Familien zu 100 % suspendiert | by_family |
| 2 Familien (E + H) bleiben strukturell unkatchbar | by_leap_class |
| v3.14 recall = 1.000 (kein valider Chain verloren) | v314 benchmark |
| v1.5 precision = 1.000 | v15 benchmark |
| v3.13 manipulation_detection = 1.000 | v313 benchmark |
| v2.7 reconstruction hash bit-identisch | gate 5 |

### Drei Lesarten der Schwelle

1. **Suspension wahrt Vorsicht ohne falsche Sicherheit.**
   In sechs Familien (B/C/D/F/G/A-subset) hat der Patch
   unmittelbar erkennbare lexikalische Merkmale, die jeden
   bekannten Angriff der Familie suspendieren *und* keinen
   v3.14-validen Chain verlieren. Die Suspension lässt das
   downstream-`_classify_gap` entscheiden, ob die Pipeline
   `BRIDGE_REQUIRED`, `GAP_DETECTED` oder
   `LOGICALLY_REJECTED` ausspricht. Keine neue Wahrheit
   wird erzeugt; alte Mechanik trägt die Folge.

2. **Inferenz ohne Marker ist Vermutung.**
   Für die zwei verbleibenden Familien (E frame_switch und H
   pure_non_sequitur) existiert kein lexikalisches Merkmal,
   das sie von v3.14's validen Chains trennt — beide
   Korpustypen haben dieselbe strukturelle Form (3 ATOMIC
   Premises + Therefore + ATOMIC Conclusion, null
   Token-Overlap). Wenn die Regel hier `CAUSAL_CHAIN` matchen
   würde, wäre die Verdikt-Begründung "der Text hat die
   richtige Form" — was bei v3.15-E/H zu falscher Sicherheit
   führt. Die Regel matchet sie weiter (kein lexikalischer
   Block möglich), aber die `v3.13`-Routing-Schicht
   davor fängt 81 / 93 davon (combined_false_support fällt von
   93 % auf 12 % im deployed Stack — v3.13 Reflexion).

3. **Die Hard-Gate-Failure ist nicht ein Versagen des
   Patches, sondern ein Versagen der lexikalischen Klasse.**
   Der Patch hält an seinen vier von fünf Gates fest und
   reduziert die strukturell-blockbaren Angriffe um 100 %.
   Gate 1 fällt, weil die strukturelle Lückenklasse
   (semantische Non-Sequiturs ohne Marker) **per
   Konstruktion** nicht durch Marker schließbar ist. Eine
   ehrliche Empfehlung sagt das.

### Die operative Antwort

> **Suspension ist intelligenter als Inferenz an genau dem
> Punkt, an dem ein strukturell-matching-Verdikt die Frage
> nach semantischer Validität *vortäuscht*. Eine Regel, die
> ein Frame-Switch-Chain als CAUSAL_CHAIN ausspricht, behauptet
> implizit, daß die Premise-Conclusion-Sequenz kausal
> kohärent ist — eine Behauptung, die strukturelle Marker
> nicht stützen können. Die Suspension verweigert genau diese
> implizite Behauptung.**

Die operative Schwelle ist meßbar:

| Eigenschaft | strukturell entscheidbar? | v3.16 Antwort |
| --- | :-: | --- |
| Negationsmarker präsent | ja | Suspend |
| Quantor-Synonym präsent | ja | Suspend |
| Authority-Verb präsent | ja | Suspend |
| X-ist-Y-Metapher präsent | ja | Suspend |
| Cycle-Synonym präsent | ja | Suspend |
| Number in beiden Seiten | ja | Suspend |
| Frame-Switch (semantisch) | **nein** | Match (Layer davor blockiert) |
| Non-Sequitur (semantisch) | **nein** | Match (Layer davor blockiert) |

Die Tabelle ist nicht philosophisch — sie ist die ehrliche
Aufstellung dessen, was Markerlisten leisten können und was
sie nicht können.

### Warum recommended_next NONE ist

Die Direktive setzt `false_support_count = 0` als bindendes
Hard Gate. Der Patch erreicht 24 (von 93). Per
success-criterion 6 → `recommended_next = NONE` →
"patch rejected". Die Runtime ist trotzdem modifiziert; was
"rejected" hier heißt: die Empfehlung sagt nicht "deploy this
as final answer". Der Patch lebt als Zwischenstufe.

### Was ehrlich übrig bleibt

* **E und H** brauchen entweder semantische Kohärenz-Checks
  (kein Markerthema mehr) oder die v3.13-Routing-Schicht muß
  bereits davor 100 % der Frame-Switches absorbieren —
  v3.13 schafft 81/93 chain-only Angriffe (v3.15-Befund).
* Die Lückenklasse ist nicht "v3.16 ist unzureichend",
  sondern "Marker-Suspension hat ein Ceiling". Das war auch
  v3.14's Lehre: strukturelle Robustheit ≠ semantische
  Robustheit. v3.16 macht die strukturelle Lücke um 74 %
  kleiner; mehr ist mit Markern nicht erreichbar.

> **Suspension ist intelligenter als Inferenz immer dann,
> wenn die Inferenz-Mechanik die zu beantwortende Frage
> strukturell nicht entscheiden kann. v3.16 zieht diese
> Linie für sechs Familien sauber und gibt für zwei Familien
> ehrlich zu, daß ihre Discrimination ein anderes Werkzeug
> braucht.**

---

## Verbote — alle eingehalten

| Verbot | Status |
| --- | --- |
| Keine neuen ClaimStates | ✅ (Suspension via existing `_classify_gap`) |
| Keine neuen Frames | ✅ |
| Keine neuen regex buckets | ✅ (extending tuples in same module) |
| Keine Benchmark-Edits | ✅ |

---

## Erfolgskriterien

| # | Kriterium | Status |
| --- | --- | ---: |
| 1 | v3.15 false_support_count = 0 | ❌ 24 |
| 2 | v3.14 recall ≥ 0.85 | ✅ 1.000 |
| 3 | v1.5 precision = 1.0, FP = 0 | ✅ |
| 4 | v3.13 manipulation_detection = 1.0 | ✅ |
| 5 | All prior replay hashes bit-identical | ✅ |
| 6 | If gate 1 fails → recommended_next = NONE | ✅ NONE |

**Patch outcome: rejected by gate 1. Reported honestly.
Runtime modification preserved; deployment recommendation
withheld.**

Replay-Hash: `1f4e5f85c085d32f`
