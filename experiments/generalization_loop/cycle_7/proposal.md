# Cycle 7 — README cross-link to generalization loop

## Self-diagnosis

The top-level `README.md` documents the self-improvement loop but
doesn't mention the generalization loop's existence.

## Change

`README.md` gains a "Generalization loop" section and a per-cycle
ledger that points to `experiments/generalization_loop/cycle_N/`.
