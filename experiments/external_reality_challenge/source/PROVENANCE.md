# Source provenance

These files are verbatim copies of:

- `https://github.com/hstre/DES.git`  (commit at clone time was `HEAD` of `main`)
  - `des_state.json` — root of the upstream repo
  - `des_prototype/des_state.json` — prototype subdir

Cloned to `/tmp/DES-upstream` during the external-reality-challenge
on branch `experiment/desi-external-reality-challenge`. They are
unmodified. They were authored by the DES program independent of
DESi development. DESi has never seen them; no DESi rule was
calibrated against them.
